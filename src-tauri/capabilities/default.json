{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set: allow invoking our custom commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
